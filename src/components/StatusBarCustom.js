import React, {useState} from 'react';
import {
  StyleSheet,
  ScrollView,
  SafeAreaView,
  Text,
  TextInput,
  StatusBar,
} from 'react-native';

export default () => {
  return (
    <SafeAreaView>
      <Text style={{backgroundColor: 'red'}}>asd</Text>
    </SafeAreaView>
  );
};
